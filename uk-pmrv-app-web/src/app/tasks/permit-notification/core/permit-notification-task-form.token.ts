import { InjectionToken } from '@angular/core';
import { FormGroup } from '@angular/forms';

export const PERMIT_NOTIFICATION_TASK_FORM = new InjectionToken<FormGroup>('Permit notification task form');
